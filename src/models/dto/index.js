const newClienteDTO = require('./new-cliente-dto')
const newImageDTO = require('./new-image-dto')
const newProdutoDTO = require('./new-produto-dto')

module.exports = {
  newClienteDTO,
  newImageDTO,
  newProdutoDTO
}
